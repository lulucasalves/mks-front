import { colors } from '../../styles/colors'
import styled from 'styled-components'

export const LogoDiv = styled.div`
  h2 {
    font-weight: 600;
    font-size: 40px;
    line-height: 19px;
    color: ${colors.white};
    margin-right: 10px;
  }

  p {
    font-weight: 300;
    font-size: 20px;
    line-height: 19px;
    color: ${colors.white};
  }
`
